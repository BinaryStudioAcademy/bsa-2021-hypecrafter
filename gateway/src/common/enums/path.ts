export enum Path {
  User = '/users',
  Auth = '/auth',
  Notification = '/notifications'
}

export enum AuthApiPath {
  Login = '/login',
  Token = '/token',
  TokenReject = '/token/reject',
  Register = '/register'
}
