export enum Path {
  Topic = '/topics',
  User = '/users',
  Auth = '/auth',
  Notification = '/notifications',
  Payment = '/payment',
  Project = '/projects',
  Tag = '/tags',
  Category = '/categories'
}

export enum AuthApiPath {
  Login = '/login',
  Token = '/token',
  TokenReject = '/token/reject',
  Register = '/register'
}
