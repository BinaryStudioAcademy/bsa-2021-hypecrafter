export enum Path {
  Topic = '/topics',
  User = '/users',
  Auth = '/auth',
  Notification = '/notifications',
  Category = '/categories',
  Payment = '/payment',
  Project = '/projects'
}

export enum AuthApiPath {
  Login = '/login',
  Token = '/token',
  TokenReject = '/token/reject'
}
