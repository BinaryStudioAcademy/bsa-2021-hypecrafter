export enum Path {
  Topic = '/topics',
  User = '/users',
  Auth = '/auth',
  Notification = '/notifications',
  Project = '/projects',
  Category = '/categories'
}

export enum AuthApiPath {
  Login = '/login',
  Token = '/token',
  TokenReject = '/token/reject'
}
