export enum Path {
  Topic = '/topics',
  User = '/users',
  Auth = '/auth',
  Notification = '/notifications',
  Project = '/projects',
  Invalid = '/*'
}

export enum AuthApiPath {
  Login = '/login',
  Token = '/token',
  TokenReject = '/token/reject'
}
